{
  "name": "twitch-points-collector",
  "version": "1.1.0",
  "description": "Chrome extension for automatic Twitch reward claiming",
  "main": "background.js",
  "scripts": {
    "build": "echo 'No build process needed'",
    "test": "echo 'No tests specified'",
    "lint": "eslint *.js",
    "pack": "zip -r extension.zip . -x '*.git*' 'node_modules/*' '*.md' 'package*.json'"
  },
  "keywords": [
    "twitch",
    "chrome-extension",
    "rewards",
    "automation"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}

